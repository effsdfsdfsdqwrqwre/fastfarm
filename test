local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/scllll099/KTA/refs/heads/main/ui", true))() 

local window = library:AddWindow("INVINCIBLE ┃ PRIVATE KILLING ", {
    main_color = Color3.fromRGB(0, 0, 0),
    min_size = Vector2.new(500, 870),
    can_resize = false,
})
